# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- пользователи могут удалённо включать/выключать отопление в своих домах;
- система поддерживает удаленную регистрацию/обновление/удаление датчиков.

**Мониторинг температуры:**

- обеспечивает хранение текущих показателей с датчиков;
- система получает данные о температуре с датчиков, установленных в домах;
- пользователи могут просматривать текущую температуру в своих домах через веб-интерфейс;

### 2. Анализ архитектуры монолитного приложения

Перечислите здесь основные особенности текущего приложения: какой язык программирования используется, какая база данных, как организовано взаимодействие между компонентами и так далее.

* Язык программирования: Go
* База данных: PostgreSQL
* Архитектура: Монолитная, все компоненты системы (обработка запросов, бизнес-логика, работа с данными) находятся в рамках одного приложения.
* Взаимодействие: Синхронное, запросы обрабатываются последовательно.
* Масштабируемость: Ограничена, так как монолит сложно масштабировать по частям.
* Развертывание: Требует остановки всего приложения.
* Архитектура внутри приложения: слоистая (слой АПИ, сервисный слой, слой взаимодействия с БД)

### 3. Определение доменов и границы контекстов

#### Домен "Управление пользователями"
* регистрация пользователей;
* хранение и предоставление информации о пользователях;
* авторизация и аутентификация;
* управление полномочиями, разграничение прав доступа;

#### Домен "Управление устройствами"
* Поддомен "Инвентаризация домов, устройств"
  * управление, хранение и предоставление информации о домах;
  * подключение и регистрация устройств;
  * инвентаризация устройств;
  * обновление информации об устройства;
  * управление группировкой/размещением устройств;
  * предоставление информации об устройствах.
* Поддомен "Мониторинг и телеметрия":
  * получение показателей с датчиков устройств;
  * хранение и предоставление показателей устройств.
* Поддомен "Интерактивное управление устройствами":
  * удаленное управление функциями устройств;
  * история событий/команд.

#### Домен "Управление сценариями"
* Поддомен "Администрирование сценариев":
  * создание/удаление/конфигурирование сценариев;
  * хранение и предоставлении информации о пользовательских сценариях.
* Поддомен "Исполнение сценариев":
  * исполнение сценариев "по-расписанию";
  * исполнение сценариев "по-триггеру".

#### Домен "Аудит действий пользователя и устройств"
* хранение/предоставление информации о действиях пользователя;
* хранение/предоставление информации о событиях устройств;

### **4. Проблемы монолитного решения**

- в случае расширения объема функциональности приложения - невозможность обновлять функциональность независимо;
- в случае расширения объема команды - усложнение разработки ПО, повышение риска ошибок;
- в случае увеличения профилей нагрузки могут возникнуть сложности при необходимости масштабировать компоненты системы.

Текущее решение не вызывает проблем по причине его ограниченной функциональности, конечно-малого 
количества профилей нагрузки и отсутствия нефункциональных требований к системе: пропускная способность (в разрезе профиля),
RTO, RPO, масштабируемость и т.д.

### 5. Визуализация контекста системы — диаграмма С4

* [C4 AS IS](.doc/as_is/ctx.puml)

# Задание 2. Проектирование микросервисной архитектуры

В этом задании вам нужно предоставить только диаграммы в модели C4. Мы не просим вас отдельно описывать получившиеся микросервисы и то, как вы определили взаимодействия между компонентами To-Be системы. Если вы правильно подготовите диаграммы C4, они и так это покажут.

**Диаграмма контейнеров (Containers)**

* [C4 TO BE Container](.doc/to_be/container.puml)

**Диаграмма компонентов (Components)**

Добавьте диаграмму для каждого из выделенных микросервисов.

* [C4 TO BE Component audit_events_service](.doc/to_be/components/audit_events_service.puml)
* [C4 TO BE Component auth_service](.doc/to_be/components/auth_service.puml)
* [C4 TO BE Component device_manager_service](.doc/to_be/components/device_manager_service.puml)
* [C4 TO BE Component device_monitoring_service](.doc/to_be/components/device_monitoring_service.puml)
* [C4 TO BE Component scenario_executor_service](.doc/to_be/components/scenario_executor_service.puml)
* [C4 TO BE Component scenario_manager_service](.doc/to_be/components/scenario_manager_service.puml)

**Диаграмма кода (Code)**

Добавьте одну диаграмму или несколько.

# Задание 3. Разработка ER-диаграммы

Добавьте сюда ER-диаграмму. Она должна отражать ключевые сущности системы, их атрибуты и тип связей между ними.

* [ER](.doc/to_be/er.puml)

# Задание 4. Создание и документирование API

### 1. Тип API

Укажите, какой тип API вы будете использовать для взаимодействия микросервисов. Объясните своё решение.

* взаимодействие WEB-интерфейса пользователей производится с использованием REST-API - стандартная популярная практика, допустимо расширить впоследствии на использование web-socket'ов с целью реализации функции отображения показателей близко к реальному времени;
* вызов активных команд устройств так же производится посредством REST-API, между сервисами допустимо перевести взаимодействие на очереди, однако ввиду необходимости сразу получить ответ на запрос и с целью упрощения разработки, необходимости так проектировать смысла не имеет; в условиях повышения нагрузок от пользовательских сценариев допустимо рассмотреть смену API на асинхронное;
* сохранение истории событий - асинхронный процесс и реализуется посредством использования транспорта (Kafka), нет необходимости дожидаться ответа об успешности публикации пользовательского события, так же отсутствует необходимость гарантировать доставку ввиду слабой важности поставляемых сведений;
* передача задач на исполнение при создании сценария - асинхронный процесс, ввиду наличия запланированных и повторяемых задач/сценариев, работающих по триггеру, количество задач может превышать выделенные ресурсы, очередь поможет произвести разгрузку в пики нагрузки.

### 2. Документация API

* [OpenAPI](.doc/to_be/openapi.yaml)

# Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

1) сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

```
	// If no location is provided, use a default based on sensor ID
	if location == "" {
		switch sensorID {
		case "1":
			location = "Living Room"
		case "2":
			location = "Bedroom"
		case "3":
			location = "Kitchen"
		default:
			location = "Unknown"
		}
	}

	// If no sensor ID is provided, generate one based on location
	if sensorID == "" {
		switch location {
		case "Living Room":
			sensorID = "1"
		case "Bedroom":
			sensorID = "2"
		case "Kitchen":
			sensorID = "3"
		default:
			sensorID = "0"
		}
	}
```

2) Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

3) Кроме того для smart_home приложения требуется база данных - добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

- Create Sensor
- Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.


